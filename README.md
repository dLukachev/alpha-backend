Короче
Кеш есть, реббит есть, воркер для реббита есть.

сейчас

Пост запрос с данными (вот тут мы получаем айди запроса) -> попадает в очередь реббита -> попадает к воркеру -> 
-> функция выполняется и сама записывает результат в редис

Хочу сделать:

Пост запрос с данными, проверка, были ли такие же данные, если были, то отдаем уже готовый результат, если нет то -> 
попадает в очередь реббита -> попадает к воркеру -> функция выполняется и сама записывает результат в редис

А так же, помимо редиса должна быть запись результата в бд, с нормальной структурой.
Придумать как можно будет идентифицировать того, кто делает запрос


сейчас для работы надо:
1. запустить бэк
2. запустить реббит в докере
3. запустить воркера